package com.Instruments;

public class Validator {

    public static boolean isValid(String ip) {
        // TODO need to add ip check
        return true;
    }

    public static boolean isValid(int port) {
        // TODO need to add port check
        return true;
    }
}
